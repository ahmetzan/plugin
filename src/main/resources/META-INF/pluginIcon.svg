<svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" viewBox="0 0 64 64">
    <!-- Arka plan (mor) -->
    <rect width="64" height="64" rx="12" fill="#6C47FF"/>

    <!-- IntelliJ plugin tarzı köşegen süs -->
    <polygon points="0,48 16,64 0,64" fill="#4B28CC"/>
    <polygon points="64,16 48,0 64,0" fill="#4B28CC"/>

    <!-- Yazı kutusu -->
    <rect x="12" y="20" width="40" height="24" rx="4" fill="white" opacity="0.15"/>

    <!-- NEXT yazısı -->
    <text
            x="-15%"
            y="50%"
            text-anchor="middle"
            dominant-baseline="central"
            font-family="Arial, Helvetica, sans-serif"
            font-size="16"
            font-weight="bold"
            fill="white"
    >
        next
    </text>
</svg>
